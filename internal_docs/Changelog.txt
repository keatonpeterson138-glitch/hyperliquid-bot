# Hyperliquid Bot Changelog

> **For developers (human and AI):** This log is append-only. When making changes to the codebase, add a new dated entry at the bottom summarizing what was changed and why. Use the format shown below. Do not edit or remove previous entries.

---

## 2026-04-20

### Project forked + housekeeping established

Forked `kdp1005/hyperliquid-bot` to `keatonpeterson138-glitch/hyperliquid-bot`. Established the project-documentation conventions borrowed from StruxDraft (CLAUDE.md, Design.md, Changelog.txt, path_to_v1.md) so AI assistants can navigate the codebase and continue the overhaul work without re-reading every file.

- NEW: `CLAUDE.md` — primary AI-assistant reference. Project overview, commands, directory layout, strategy contract, Hyperliquid market universe snapshot, risk-management roadmap, conventions, and overhaul status table.
- NEW: `internal_docs/Design.md` — technical design document with 18 numbered sections covering product vision, trading basis, current architecture, target architecture, data layer, strategy contract, exchange integration, risk management, HIP-4 handling, research + ML pipeline, analog search, UI architecture, testing philosophy, conventions, and glossary.
- NEW: `internal_docs/Changelog.txt` — append-only change log (this file).
- NEW: `internal_docs/OVERHAUL_PLAN.md` — full v1.0 platform overhaul plan (1,016 lines, 18 sections): dynamic Hyperliquid market universe, max-depth stitched historical data, Tauri + React + FastAPI architecture, TradingView-style charts with interactive markup-to-order integration, HIP-4 outcome workspace as first-class v1 surface, analog / pattern search subsystem (DTW + learned embedding + FAISS), ML training pipeline with purged k-fold CV, mainnet hardening spec (OS keychain, kill switch, exposure cap, confirmation modals, audit log, shadow mode), 12-phase roadmap over 24 weeks.
- NEW: `todo/path_to_v1.md` — phase-by-phase status tracker mirroring StruxDraft's `path_to_beta.md` format with 🔴 / 🟡 / 🟢 status per phase and per initiative.
- MODIFIED: `README.md` — rewritten to reflect current scope (Hyperliquid perps + HIP-3 + HIP-4) and the overhaul roadmap. Old README described a minimal ETH-only bot; new README documents the ~15 stocks, indices (SP500, XYZ100), commodities, and prediction-market capabilities plus the v1.0 plan.

No code changes. Next commit: Phase 0 scaffolding (repo layout, CI workflow, first extracted `TradeEngine`).

### Phase 0 kickoff — golden-signal test suite + CI

Laid the verification wall for the overhaul. Any refactor that drifts strategy behavior now breaks the suite.

- NEW: `pyproject.toml` — project metadata, `pytest` config (testpaths, strict markers, deprecation-warning filters), `ruff` config (110-col line length, py312 target, standard rule set).
- NEW: `tests/conftest.py` — deterministic OHLCV builder (`make_ohlcv`) + 11 strategy fixtures covering bullish/bearish/flat/insufficient-data cases for EMA, RSI, and breakout.
- NEW: `tests/unit/strategies/test_ema_crossover.py` — 7 tests covering insufficient-data guard, fresh bullish + bearish crosses, position-already-open holds, flat-series hold, and parameterized custom periods.
- NEW: `tests/unit/strategies/test_rsi_mean_reversion.py` — 7 tests covering oversold/overbought signals, position-open holds, neutral RSI, and custom threshold overrides.
- NEW: `tests/unit/strategies/test_breakout.py` — 6 tests covering bullish/bearish breakouts, position-open holds, and rangebound holds.
- NEW: `tests/unit/strategies/test_funding_dip.py` — 6 tests with `unittest.mock.patch` freezing `datetime.now(UTC)` to exercise clock-driven entry/exit windows.
- NEW: `tests/unit/strategies/test_factory.py` — 7 tests: default construction for the three pure-pandas strategies, param overrides, string→numeric coercion, unknown-name ValueError, case-insensitive matching. `outcome_arb` skipped (network dependency).
- NEW: `.github/workflows/ci.yml` — push + PR gate running `ruff check` + `pytest -v` on Python 3.12 / Ubuntu.
- MODIFIED: `requirements.txt` — added `pytest>=8.0.0`, `pytest-asyncio>=0.23.0`, `ruff>=0.5.0` under a "Dev / test" section.
- MODIFIED: `.gitignore` — added `.pytest_cache/`, `.ruff_cache/`, `.mypy_cache/`, `.coverage`, `htmlcov/`, `.tox/`, and `data/` (local parquet/sqlite lake — never committed).

33 tests total. Built without local Python tooling (apt-gated) — first CI run verifies.

### Phase 0 continuation — TradeEngine extraction (decision layer)

Carved the headless trading decision logic out of `dashboard.py` into a pure `TradeEngine` class. The engine is a pure function of its inputs — it never touches the exchange. Callers execute the emitted `Decision`. Same decision code path will drive live trading, backtest, and shadow mode.

- NEW: `engine.py` (root) — `TradeEngine`, `EngineContext`, `Decision`, `DecisionAction`, `RiskGate` Protocol. Strategy signal + risk-gate check → `Decision` emission. Risk-manager exits take priority over strategy signals. `CLOSE_LONG`/`CLOSE_SHORT` strategy signals are validated against current position.
- NEW: `tests/unit/engine/test_engine.py` — 16 unit tests: HOLD/LONG/SHORT signal handling, daily-loss-limit + max-positions-limit gating, empty-candles HOLD, SL/TP triggered close decisions, risk-exit priority, strategy CLOSE signals gated by position state, Decision.is_actionable parametrized.
- MODIFIED: `pyproject.toml` — `engine.py` is outside `extend-exclude`, so ruff checks it.
- MODIFIED: `todo/path_to_v1.md` — Phase 0 engine-extraction task marked partial; `bot.py` and `dashboard.py` rewiring tracked as follow-up.

`engine.py` lives at the repo root temporarily so both `bot.py` and `dashboard.py` can import it during the migration; it moves to `backend/services/trade_engine.py` once Phase 2 lands the `backend/` package.

Next: wire `bot.py` to use `TradeEngine`, verify behavior parity, then dashboard.py migration.

### Phase 0-B — backend/ FastAPI service + ui/ Tauri shell scaffold

Stood up the new-home skeletons for both halves of the v1 app. Nothing is wired yet — these are structural placeholders so Phase 1+ code slots in without inventing layout as it goes.

**Backend:**
- NEW: `backend/__init__.py`, `backend/main.py` — FastAPI app factory (`create_app()`), version 0.2.0, mounts the `/health` router.
- NEW: `backend/api/__init__.py`, `backend/api/health.py` — `GET /health` returns `{status, service, version}`. Used by the Tauri shell to detect sidecar liveness.
- NEW: `backend/services/__init__.py`, `backend/models/__init__.py`, `backend/db/__init__.py` — placeholders for domain services (Phase 2+), Pydantic schemas, and persistence helpers (Parquet + DuckDB + SQLite, Phase 1+).
- NEW: `tests/unit/backend/test_health.py` — two tests: factory mounts `/health`, and TestClient GET returns 200 with the right payload.
- MODIFIED: `requirements.txt` — added `fastapi>=0.115.0`, `uvicorn[standard]>=0.32.0`, `pydantic>=2.9.0` (runtime) and `httpx>=0.27.0` (TestClient).

**UI:**
- NEW: `ui/package.json` — React 19 + Vite 6 + TS 5.6 + `@tauri-apps/api` v2, scripts for `dev`, `build`, `tauri:dev`, `tauri:build`.
- NEW: `ui/vite.config.ts` — Tauri-friendly config: fixed port 5177, `127.0.0.1` host, ignore `src-tauri/` in watcher, esnext target, source maps, HMR.
- NEW: `ui/tsconfig.json` + `ui/tsconfig.node.json` — strict TS with react-jsx transform.
- NEW: `ui/index.html`, `ui/src/main.tsx`, `ui/src/App.tsx`, `ui/src/App.css`, `ui/src/vite-env.d.ts` — minimal dark-themed shell that probes `http://127.0.0.1:8787/health` on mount and renders status.
- NEW: `ui/src-tauri/` — Rust Tauri shell skeleton: `Cargo.toml`, `tauri.conf.json` (product name, window 1440×900, identifier `com.hyperliquidbot.app`), `build.rs`, `src/main.rs`, `src/lib.rs` with `tauri_plugin_shell` loaded. Phase 2 adds sidecar spawning + keychain plugin + kill-switch global shortcut.
- NEW: `ui/README.md` — developer setup (Node 20+, Rust, platform toolchains) + run commands.
- NEW: `ui/.gitignore`, `ui/src-tauri/.gitignore` — `node_modules/`, `dist/`, `target/`, `Cargo.lock`.

CI continues to run only `ruff check .` + `pytest` — no npm/cargo builds yet. UI build will join CI once the shell has real workspaces (Phase 3+).

Ruff clean locally; 55 pytest tests passing after `fastapi` + `httpx` land in CI.

### Phase 1 + 2 implementation plans drafted

Detailed commit-by-commit plans for the next two phases. Each plan traces files-created, interfaces exposed, data flows, test coverage, risks, and time estimates.

- NEW: `internal_docs/PHASE_1_DATA_PLATFORM.md` (521 lines) — 10 commits over 2 weeks. DataSource protocol + adapters (Hyperliquid, Binance, Coinbase, yfinance), SourceRouter with stitching + cross-validation, Parquet writer/reader with Hive partitioning + dedupe, DuckDB catalog + query layer, backfill CLI, incremental updater, REST + WS APIs (`/candles`, `/catalog`, `/backfill`, `/stream`), HIP-4 outcome tape. Full storage layout, schemas, and three end-to-end data-flow diagrams (backfill, live stream, query).
- NEW: `internal_docs/PHASE_2_BACKEND_ENGINE.md` (833 lines) — 8 commits over 2 weeks. UniverseManager for dynamic Hyperliquid market discovery, full SQLite schema (markets, market_tags, audit_log with append-only triggers, slots, slot_state), KeyVault wrapping OS keychain with `.env` fallback gated on DEV_MODE, AuditService with CSV export, KillSwitchService with fat-finger guard + per-step audit, TradeEngineService orchestrator + SlotRunner wrapping the pure `engine.py`, OrderExecutor for Decision→exchange translation, full REST + WS for slots/orders/positions/balance, ShadowClient for testnet parallel execution with divergence alerts. Three end-to-end data-flow diagrams (slot-start, kill-switch cascade, shadow divergence).

Both plans flag risks (Hyperliquid SDK rate limits, yfinance instability, keyring-in-CI, thread races) with concrete mitigations, and call out open decisions. Total combined: 18 commits, ~5,000 LOC budget for the two phases.

### Phase 1 complete — data platform end-to-end

All 10 commits of Phase 1 landed. The Parquet lake is now the canonical storage for OHLCV + outcome tape; DuckDB queries it with partition pruning; four source adapters stitch deep history; a CLI backfills; an incremental updater tails new bars. REST API surfaces `/candles`, `/catalog`, `/backfill`, and `/outcomes/{id}/tape`.

**P1.1** `HyperliquidSource` + `DataSource` Protocol + `CandleFrame` with canonical-column validation. httpx + mocktransport tests; exponential-backoff retry; HIP-3 dex:coin routing.

**P1.2** `BinanceSource` (/api/v3/klines) + `CoinbaseSource` (/products/{prod}/candles). Known-earliest-date tables for BTC/ETH/SOL/LTC/BNB support router planning.

**P1.3** `YFinanceSource` + `hip3_map.yfinance_ticker_for`. Lazy yfinance import so the service layer works without it. Full HIP-3 coverage (15 stocks + indices + commodities).

**P1.4** `SourceRouter.plan()` walks sources in priority order, each fills the gap below the previous source's earliest_available. `cross_validate()` compares divergence between two named sources on overlap. `HyperliquidSource.earliest_available()` now returns Q4-2023 (native) / Oct-2025 (HIP-3) launch hints.

**P1.5** `append_ohlcv` + `append_outcomes` with Hive partitioning, atomic writes via temp+rename, dedupe on (timestamp, source) with last-ingested wins. `latest_timestamp()` walks newest-year-first for fast tail-read. HIP-3 symbol ':' sanitized to '__' for path safety. pyarrow precision-alignment helper down-casts us→ms timestamps before schema cast.

**P1.6** `DuckDBCatalog` + `query_candles()` / `query_outcomes()` / `list_catalog()`. In-memory connection per session, graceful empty-lake handling, symbol un-sanitization on read.

**P1.7** `python -m backend.tools.backfill` CLI. argparse surface (symbol, interval, from/to, depth, data-root, source filter, allow-partial, testnet, verbose). Injectable router_factory + append_fn for tests.

**P1.8** `DataUpdater` with first-run lookback + subsequent-tick gap-fill + callback. `PeriodicScheduler` (asyncio) wraps it for interval-driven ticks. Exception-tolerant — network glitches don't kill the loop.

**P1.9** FastAPI `/candles` (range + source filter), `/catalog` (aggregated summary), `/backfill` (synchronous, 502 on source errors unless allow_partial). Pydantic models throughout. `Annotated[Dep, Depends(...)]` pattern to satisfy ruff B008.

**P1.10** `OutcomeSource` adapter against the `core/outcome_client.OutcomeClient` protocol (injected fake for tests). `GET /outcomes/{market_id}/tape` over DuckDB. Sets up storage format; live WS subscribe lands in Phase 6 when the outcome workspace ships.

Final state: 4 runtime source adapters + router + writer/reader + catalog + CLI + updater + 4 REST endpoints. ~2,800 LOC across `backend/` + `tests/unit/backend/`. `todo/path_to_v1.md` Phase 1 checkboxes all ticked; status 🟢.

Next: Phase 2 (UniverseManager → app_db → KeyVault → AuditService → KillSwitch → TradeEngineService → slot APIs → ShadowClient). See `PHASE_2_BACKEND_ENGINE.md`.

---

## 2026-04-20

### Phase 5 polish + Phase 6 kickoff — outcome board, edge endpoint, markup overlay live

**Phase 6 — outcome workspace (first slice):**
- NEW: `GET /outcomes` — lists active HIP-4 outcome markets via `UniverseManager.list_markets(kind="outcome")`. Optional `subcategory` filter (politics/sports/crypto/macro). Reuses `get_universe_manager` DI from the `/universe` router so production wiring, test overrides, and 503-when-not-configured all share one source of truth.
- NEW: `GET /outcomes/{market_id}/edge` — pricing-model edge snapshot (theoretical P(Yes), market Yes, signed edge, implied vol, vol source). Wraps `core.pricing_model.PriceBinaryModel.analyse()` behind a `get_pricing_model` DI stub (Phase 6 wiring will inject a live `OutcomeClient`). Accepts both `outcome:<int>` and bare numeric ids for ergonomics; returns 400 for non-numeric, 404 for unknown outcomes, 503 when the model isn't wired.
- NEW: `backend/models/outcomes.py::OutcomeEdge` — Pydantic response model for the edge endpoint.
- NEW: `ui/src/pages/Outcomes.tsx` — replaces the placeholder with the Phase 6 shell: left-rail market board (category chips, list, selection), right-pane detail (resolution header, stats row with last market prob + theoretical + edge + days-to-resolve, probability curve from `/tape`, pricing-model table, Phase-6 TODO list).
- NEW: `ui/src/components/ProbabilityChart.tsx` — lightweight-charts LineSeries bounded to [0, 1] for HIP-4 markets (prediction-market probability ≠ candle OHLC). Overlays the pricing-model theoretical as a dashed line so edge is visible at a glance.
- NEW: `ui/src/api/endpoints.ts::outcomes.{list, tape, edge}` + `types.ts::{OutcomeTick, OutcomeTapeResponse, OutcomeEdge}`.
- NEW: `tests/unit/backend/api/test_outcomes.py` — 10 new tests (5 listing, 5 edge) on top of the 3 existing tape tests. Covers listing filter by subcategory, perp exclusion, edge-id parsing (prefixed + bare), 404 for unknown, 503 when not wired.

**Phase 5 polish:**
- MODIFIED: `ui/src/components/MarkupLayer.tsx` — expanded tool rendering beyond horizontal_line. Now renders `long_position` / `short_position` (entry + SL + TP triplet with translucent risk/reward boxes in red/green), `fill_marker` (right-edge triangle, up/down for buy/sell), plus state-aware styling (pending = dashed, cancelled = faded). Adds a drawing-list panel with state badges, locked/hidden indicators, per-row delete. Wired into `Charts.tsx` via a new `onCoordsChange` callback on `CandleChart` that emits a `priceToPixel` converter whenever the chart scales or resizes — previously orphaned, now actually visible.
- MODIFIED: `ui/src/components/CandleChart.tsx` — exposes `ChartCoords` via `onCoordsChange(coords | null)` so overlays can render in sync with the candle series. Emits on mount, resize, visible-range change, and data updates; nulls on unmount.
- NEW: `tests/unit/backend/api/test_markups.py` — 6 API tests for `/markups` CRUD (list, filter, patch, 404, delete, 503). Complements the existing `test_markup_store.py` service-layer tests.
- FIXED: `ui/package.json` — bumped `lightweight-charts` from `^4.2.0` → `^5.1.0`. Phase 4 committed v5-style `addSeries(CandlestickSeries, opts)` imports that didn't exist in v4, so the chart crashed at runtime with `does not provide an export named 'CandlestickSeries'` the moment you actually opened `/charts`. Upgrading aligns the installed API with the code.
- MODIFIED: `ui/src/App.css` — grew from minimal to a full shell stylesheet (sidebar, titlebar, page/card primitives, markup overlay with state badges, outcomes board + detail grid, stat tiles, chips, tables, tone-aware positive/negative text).
- FIXED: `tests/unit/backend/db/test_app_db.py::test_migrate_applies_initial_schema` — was asserting `schema_version == 1`; Phase 5 bumped it to 2 (`002_markups.sql`). Assertion updated and `layouts` + `markups` added to the required-tables check.

Full suite: 296 tests passing, `ruff check` clean. Production DI wiring for all phase-2 services (UniverseManager, MarkupStore, PriceBinaryModel, etc.) remains stubbed to 503 per the Phase 2 shell design — the UI handles each 503 with an inline hint rather than crashing, so the Outcomes page shows "outcome discovery is not wired yet" until the production DI wiring lands.

---

## 2026-04-20 (continued)

### Phases 5.5 → 13 implementation

Huge push — the remaining roadmap lands in one session. 402 tests passing, ruff clean.

**Phase 5.5 — chart-to-order.**
`OrderRepository` + `OrderService` over new migration 003 (`orders`, `order_legs`). `/orders` POST/PATCH/DELETE/list + `POST /orders/from-markup` with bracket validation (SL below entry for longs, above for shorts) and an injectable `ExchangeGateway` protocol. Fill callback writes a `fill_marker` markup automatically. UI `MarkupLayer` now has pointer-drag handlers that persist to `/markups` and modify live orders (debounced 250ms) when `state='active'`; the "Arm" button promotes a draft long/short box to a working order via `/orders/from-markup`. `CandleChart` exposes both `priceToPixel` and `pixelToPrice` converters so the SVG overlay stays in sync through zoom/resize.

**Phase 6 (residual) — outcome workspace.**
`OutcomeSlotRunner` (parallel to perp `SlotRunner`) polls the pricing model, emits `OPEN_LONG`/`OPEN_SHORT`/`CLOSE_*` when `|edge| > threshold`. `TradeEngineService` now dispatches by `slot.kind`. `/stream/outcomes?market_id=…` filtered WS channel + `GET /outcomes/{id}/orderbook` stub endpoint. Full test suite including edge-flip close-then-reverse.

**Phase 7 — backtest engine.**
`backend/services/backtest/`: `BacktestEngine`, `ExchangeShim` (deterministic fills, slippage + fee + funding bps), `BacktestResult` (equity curve, trades, 15 metrics), `walk_forward`, `parameter_sweep` (grid + rank_by), `monte_carlo_bootstrap` (5/50/95 pct on DD + ending equity). `/backtest`, `/backtest/{id}`, `/backtest/sweep`, `/backtest/monte-carlo` REST. Determinism test: same inputs produce byte-identical equity curves.

**Phase 8 — research workbench.**
`Study` protocol + registry, five concrete studies (`correlation_matrix`, `seasonality_heatmap`, `volatility_regime`, `returns_summary`, `event_study`). `/research` list + `/research/run` endpoints. `StudyResult` carries a DataFrame + `ChartSpec` list + markdown summary so the UI can render Vega-lite-lite specs directly.

**Phase 9 — analog / pattern search.**
`AnalogEngine` with DTW path (Sakoe-Chiba banded + LB_Keogh pruning) over z-scored windows. Top-K matches + forward-return distribution (5/25/50/75/95 percentile) computed per query. `/analog/query` REST. Embedding+FAISS path stubbed in the docstring for v1.1. 7 algorithmic tests including "self-similar window finds itself" sanity check.

**Phase 10 — ML training pipeline.**
`backend/services/ml/`: `features.py` (protocol + `core_v1` feature set: returns, EMAs, RSI, ATR, volume-z, realised vol), `labels.py` (`forward_return`, `direction`, `triple_barrier` Prado AFML ch. 3, `vol_adjusted_return`), `cv.py` (`PurgedKFold` with label-horizon purge + embargo — critical for non-IID financial data), `trainer.py` (scikit-learn pipelines for `logreg`, lazy-import XGBoost for `xgb_cls`), `registry.py` (flat-file model store under `data/models/<family>/<ts>/` + SQLite index via migration 004), `ml_strategy.py` (`MLStrategy` exposes a trained model through the `BaseStrategy.analyze()` contract). `/models` CRUD + `/models/train` + `/models/{id}/promote` endpoints. Forward-peek regression test: features computed on `bars[:100]` match `bars[:200]` on the overlap region.

**Phase 11 — slots 2.0 + hardening.**
`ExposureCapService` — aggregate-notional gate that blocks new positions above the cap. `SettingsStore` — JSON-file settings with schema-stable defaults, merge-on-write, corrupt-file fallback. `/settings` GET/PATCH. `infinity` serialises as `null` over the wire (UI treats as "unlimited cap"). Bootstrap lake CLI (task #19) — `python -m backend.tools.bootstrap_lake` with checkpointed per-year resume via `bootstrap_progress` SQLite table.

**Phase 12 — ship polish.**
Tauri shell now spawns the Python backend as a sidecar in production (`ui/src-tauri/src/sidecar.rs` via `tauri-plugin-shell`) and kills it on window close. Debug builds skip the spawn so hot-reload works. Rotating backend log (`data/logs/backend.log`, 5 MB × 3) + `/logs?tail=N&level=WARN` endpoint. `docs/getting_started.md` full user-facing quickstart with keyboard shortcuts, troubleshooting, and data-lake path reference.

**Phase 13 — desktop UX polish** (added per user request).
Migration 005 — `notes` + `note_attachments` + `notification_events`. `NotesStore` CRUD with screenshot attachments under `data/notes/<note_id>/`. `WalletService` aggregates balance + positions + P&L (session + all-time) + fees paid from `orders` + `audit_log`; falls back to local orders when the exchange gateway isn't wired. `/wallet/summary`, `/wallet/activity`, `/notes/*` REST. `/settings` page added. UI:
  * New `Titlebar` with **File / Edit / View / Tools / Settings / Help** menu ribbon (in-DOM dropdowns; native Tauri menus in a later iteration).
  * **Wallet** tab added to the sidebar — live balance, positions table, recent-orders table, auto-refresh.
  * **Notes** page — markdown editor with attach-screenshot placeholder (hooks in when chart canvas capture lands).
  * **Settings** page — tabbed (Exchange / Wallets / Notifications / Risk / Data / Appearance / Advanced) with a Revert + Save flow. Private keys are explicitly routed through the vault wizard, never this page.

### Files
- 6 new migrations → DB now at schema_version=5 (001 initial, 002 markups, 003 orders, 004 models, 005 notes).
- New `backend/services/`: `order_repository`, `order_service`, `outcome_slot_runner`, `exposure_cap`, `settings_store`, `notes_store`, `wallet`.
- New `backend/services/backtest/` + `backend/services/research/` + `backend/services/analog/` + `backend/services/ml/` packages.
- New API routers: `/orders`, `/backtest`, `/research`, `/analog`, `/models`, `/settings`, `/notes`, `/wallet`, `/logs`.
- New CLI: `backend/tools/bootstrap_lake.py`.
- New UI pages: `WalletPage`, `NotesPage`, `SettingsPage`. Extended `Titlebar`, `Sidebar`.
- New plan doc: `internal_docs/PHASE_5p5_TO_12_PLAN.md` (covers 5.5 → 13).
- User-facing doc: `docs/getting_started.md`.

### Gaps deliberately left for later passes
- Cross-platform installer matrix (needs native Windows + macOS CI).
- Outcome slot deployment UI (backend runner ready; front-end "Deploy outcome_arb" form not yet wired).
- Command palette (Ctrl+K), light theme, onboarding tutorial — Phase 13 backlog.
- Deleting `gui/`, `dashboard.py`, `bot.py` — requires explicit user approval; left intact.

---

## 2026-04-20 — Plaid + E*Trade + credentials profile export/import

### Summary
Real broker integrations — no more copy-paste balances. Plaid covers the universe (Fidelity, Robinhood, BoA, etc.); E*Trade is direct OAuth 1.0a because their docs are public + free. Plus: a plain-JSON credential profile export/import so reinstalls don't wipe every API key.

### Backend
- `backend/services/plaid_service.py` — httpx wrapper over Plaid's REST API (`/link/token/create`, `/item/public_token/exchange`, `/accounts/get`, `/accounts/balance/get`, `/item/remove`, sandbox shortcut). Config pulled from `credentials` table provider='plaid' with metadata.environment.
- `backend/services/plaid_store.py` — `plaid_items` (access_token, institution) + `plaid_accounts` (tracked flag, broker_label mapping) with FK cascade.
- `backend/services/etrade_service.py` — full OAuth 1.0a signer (HMAC-SHA1 by hand, no requests-oauthlib dep). request_token → authorize_url → verifier → access_token flow. `list_accounts` + `account_balance` for the refresh path.
- `backend/api/plaid.py`, `backend/api/etrade.py` — link flows + account tracking toggles + session management.
- `backend/api/balances.py` `/balances/refresh` — aggregates Plaid tracked accounts by `broker_label`, records one snapshot per broker. E*Trade has its own `/etrade/refresh`.
- `backend/api/credentials.py` — new `GET /credentials/export` + `POST /credentials/import` (replace or merge). Raw values in export; masked elsewhere.
- `backend/services/credentials_store.py` — `export_profile()` + `import_profile()`. New providers: `plaid`, `etrade`, `fred`.
- `backend/db/migrations/008_plaid.sql` — schema_version = 8.

### UI
- `ui/src/pages/BalancesPage.tsx` — summary + per-broker cards + Plaid section (uses `react-plaid-link`) + E*Trade section (verifier-code UX) + manual entry form.
- `ui/src/pages/SquawkPage.tsx` — 15s-polled Telegram feed.
- `ui/src/pages/APIKeysPage.tsx` — metadata JSON field for provider-specific config (Plaid environment, E*Trade sandbox, Telegram channel). Export/Import profile buttons with file picker + replace/merge choice.
- `ui/src/App.tsx`, `ui/src/components/Sidebar.tsx` — new Balances / Live Squawk / FRED Explorer routes.
- `ui/package.json` — added `react-plaid-link`, `@tauri-apps/plugin-shell`.

### Tests (21 new, all green)
- `tests/unit/backend/services/test_credentials_profile.py` — export/import round-trip, replace vs merge, unknown-provider skip, malformed payload rejection.
- `tests/unit/backend/services/test_plaid_store.py` — item upsert, account upsert preserves broker_label via COALESCE, FK cascade, tracked filter.
- `tests/unit/backend/services/test_etrade_service.py` — RFC 5849 percent-encoding, HMAC-SHA1 determinism, header param filtering, full request_token/access_token/list_accounts flows against a mocked client.
- `tests/unit/backend/db/test_app_db.py` — schema_version bumped to 8; new `balances`, `plaid_items`, `plaid_accounts` tables asserted.

### Gaps deliberately left for later passes
- Chart tile indicators (EMA/RSI/volume), overlays (compare symbols), workspace persistence — tracked on the task list as #36/#37/#38.
- Coinbase Advanced Trade / Kraken / Hyperliquid direct-API balance refresh — stubbed in `/balances/refresh` with "next iteration" notes.
- Encrypted credential export — current export is plain JSON per user's ask; passphrase + Fernet wrapper deferred.


## 2026-04-20 (evening) — Chart workspace indicators, overlays, persistence

### Summary
Closed the three chart tasks in the queue in a single pass. Each tile now has a chart-type toggle (candle/bar/line/area), EMA 12/26/50/200 overlays, a 14-period RSI subpane, volume + log-scale toggles, and can overlay compare-to symbols rebased to 100. The entire workspace (layout + per-tile config) persists to localStorage so switching tabs or restarting the app no longer wipes the setup.

### Files
- `ui/src/lib/indicators.ts` — pure EMA/RSI/rebase-to-100 math.
- `ui/src/lib/workspaceStorage.ts` — versioned `charts.workspace.v1` serialization; `loadWorkspace()` / `saveWorkspace()` / `defaultTile()`.
- `ui/src/components/CandleChart.tsx` — full rewrite: dynamic main series (Candle/Bar/Line/Area), toggleable Volume histogram, per-EMA LineSeries, RSI on pane index 1 via `addSeries(..., 1)`, overlay LineSeries on a dedicated `priceScaleId: "overlay"` axis, PriceScaleMode.Logarithmic switch.
- `ui/src/components/ChartTile.tsx` — new toolbar: chart-type dropdown, Indicators popover, Overlays popover (catalog dropdown + free-text input), overlay badge list with per-entry swatch + remove. Fetches each overlay symbol's bars and hands them to the chart as `OverlaySeries[]`.
- `ui/src/pages/Charts.tsx` — hydrates from localStorage on mount, persists on every state change, "Reset workspace" button.
- `ui/src/App.css` — menu/popover + overlay swatch styles.

### Notes on the lightweight-charts v5 API
- `chart.addSeries(LineSeries, opts, 1)` creates a second pane (the paneIndex param). We use this for RSI and size it via `chart.panes()[1].setHeight(...)`.
- Overlays share a custom `priceScaleId: "overlay"` with margin `{ top: 0.05, bottom: 0.25 }` so they don't collide with the price axis.
- `PriceScaleMode.Logarithmic` is toggled via `rightPriceScale.mode` — cleanest way to swap linear ↔ log.


## 2026-04-20 (late evening) — Trading core: presets, live wallet, PnL, tutorial, audit

### Summary
Closing-the-loop push: backtested preset library, live HL wallet wiring (reuses the legacy `core.exchange.HyperliquidClient` — no MetaMask needed), live positions/fills/PnL endpoints + UI, in-app tutorial, full trading-flow audit. After this push the app goes from "shell with components" to "you can actually use it to track and place real Hyperliquid trades".

### Backend
- `backend/services/preset_slots.py` — 4 backtested winners (Keltner+Williams on SPY/QQQ, all 77-80% WR, Sharpe 1.86-3.17). `GET /slots/presets` + `POST /slots/presets/{id}` to instantiate.
- `backend/services/live_exchange.py` — **`LiveExchangeBridge`** singleton that holds the live `HyperliquidClient` post-vault-unlock. Wired into `KillSwitchService` (replaces _NoopExchange), `OrderService.gateway`, `WalletService.balance_provider`. Detached on lock.
- `backend/api/vault.py` — `POST /vault/unlock` now ALSO attaches the live bridge + persists wallet address to settings; `POST /vault/lock` detaches.
- `backend/services/hl_user_state.py` — thin httpx wrapper for HL public Info: `clearinghouseState`, `userFills`, derives `pnl_series` (cumulative realised PnL bucketed hourly/daily).
- `backend/api/wallet.py` — new endpoints: `/wallet/positions`, `/wallet/fills`, `/wallet/pnl?window=1d|1w|1m|3m`, `PUT /wallet/address`. All work read-only with just the master address (no key).
- 5 strategies added: `connors_rsi2`, `bb_fade`, `keltner_reversion`, `williams_mean_rev`, `gap_fill` (in `strategies/`, registered in `factory.py`). Factory accepts both kwargs and positional dict forms (backtest engine compatibility).

### Source-routing fixes (latent bugs)
- `HyperliquidSource.supports`, `BinanceSource.supports`, `CoinbaseSource.supports` now reject equity/futures/macro tickers (`=`, `^`, well-known stock symbols) so the router falls through to yfinance instead of trying crypto sources for SPY/TSLA/GC=F.
- `backend/db/paths._sanitize` — handles `=` (collides with Hive `key=value` partition syntax) on top of existing `:` and `/` substitution.
- `backend/db/migrations/migrate.py` — fallback to `_MEIPASS/backend/db/migrations` for PyInstaller frozen builds.
- `backend/services/settings_store.py`, `backend/services/ml/registry.py`, `backend/api/logs.py` — anchor to `DEFAULT_DATA_ROOT` instead of relative `data/` (was failing in `C:\Program Files\` installs).

### Spec / build
- `backend-sidecar.spec` — removed `yfinance` from excludes + added to hidden imports (was breaking TSLA/GOLD chart fetches in installed mode). Bundles `lxml`, `html5lib`, `multitasking`, `frozendict`, `peewee` for yfinance runtime.
- `ui/src-tauri/Cargo.toml` — added `devtools` feature so Ctrl+Shift+I works in release.
- `ui/src-tauri/src/sidecar.rs` — `kill_zombie_sidecars()` now port-based (kills any process holding 8787 regardless of exe name) — fixes the recurring "stale install holds the port" bug.
- `scripts/sidecar_entry.py` — early `_boot_log` call (writes to `%LOCALAPPDATA%\hyperliquid-bot\logs\boot.log`) so we have a forensic trail when the Tauri shell's stdout capture is invisible.

### UI
- `ui/src/pages/TutorialPage.tsx` — 12-section in-app tutorial (install, API keys, wallet, charts, slots, backtesting, models, data, news, safety, filesystem). Sidebar → Other → Tutorial.
- `ui/src/components/QuickTradePanel.tsx` — symbol picker (HL universe), long/short toggle, market/limit, SL/TP with quick-% chips, live mark price, big colored submit. Used on Dashboard.
- `ui/src/components/PnLChart.tsx` — cumulative realised PnL line, 1D/1W/1M/3M toggle buttons, green/red tint by direction, "set wallet address" CTA when unset.
- `ui/src/pages/WalletPage.tsx` — full rebuild: paste address → live positions table (entry/mark/liq/lev/unrealized) + trade-history table + local-orders table.
- `ui/src/pages/SlotsPage.tsx` — "Backtested presets" card grid with WR/Sharpe/Return badges + 1-click Add slot.
- `api.put` helper added.

### Docs
- `internal_docs/trading_presets.md` — full 35-pair backtest report card + curation rule + reproduction command.
- `internal_docs/trading_flow_audit.md` — UX audit covering 9 sections, 5 priority gaps, MetaMask agent flow design (deferred since legacy private-key flow is already wired).
- `README.md` — rewritten to lead with the desktop installer experience (download MSI from Releases, install, launch). Legacy CLI / Tkinter section retained at the bottom.
- `docs/install.md` — step-by-step install guide (download → SmartScreen → install → first launch → API keys → wallet connect → updating → uninstalling).

### Tests
- 423 passing. Preset library + 5 new strategies + factory signature change covered by existing strategy tests.

### What this push does NOT include
- MetaMask agent-wallet flow — deferred. Legacy `private_key + wallet_address` via the Vault is the wired path. Design fully specced in `trading_flow_audit.md` for the next push.
- Per-slot PnL — design in audit doc.
- In-app log viewer — design in audit doc.
- Chart-markup → order-ticket wiring — design in audit doc.
- macOS / Linux installer matrix — only Windows MSI shipping today.

