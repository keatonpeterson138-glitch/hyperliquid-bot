{
  "version": 1,
  "note": "Copy this to backend/config/credentials_seed.json (gitignored) and fill in the api_key values you want baked into the installer. On first boot (empty credentials table) the sidecar imports this file. Skip any provider you don't want to pre-fill.",
  "credentials": [
    {
      "provider": "fred",
      "label": "default",
      "api_key": "REPLACE_WITH_YOUR_FRED_API_KEY",
      "metadata": {}
    },
    {
      "provider": "alpha_vantage",
      "label": "default",
      "api_key": "REPLACE_WITH_YOUR_ALPHAVANTAGE_API_KEY",
      "metadata": {}
    },
    {
      "provider": "coingecko",
      "label": "free",
      "api_key": "",
      "metadata": {}
    }
  ]
}
